import { primaryFont, fontScale } from "./Typography";
import { blue, green, red } from "./Color";

export const defaultTheme = {
  primaryColor: blue[300],
  secondaryColor: green[200],
  tertiaryColor: red[100],
  fontSize: fontScale.h4,
  primaryFont,
};
